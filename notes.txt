retract% will have to take the whole list. if it doesn't and retract is called with
list of numbers, it can act very strangely, because after each retraction, the list
is reindexed.

domluvit s dostalem, jestli je treba zachovavat kompatibilitu s clipsem co do
indexovani faktu cisly (front-end:facts, retract), co do filtrovani faktu
ve znalostni bazi by bylo lepsi napr. filtrovani faktu podle templatu,
prip. napr. vypsat fakta konsistentni s patternem

recursive tree-node printing:
(defun print-node (node depth &optional (stream t))
  (dotimes (i (1- depth))
    (format stream " "))
  (format stream " ~[*-- ~:; `-- ~]~A~%" depth node))

get list of class methods in sbcl:
(sb-mop:specializer-direct-methods (find-class 'my-object))
