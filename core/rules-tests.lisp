(in-package :core-tests)
#|
(defclass rule-tests (test-case) ())

(defmethod set-up ((tests rule-tests)))

;; not much to test here

(add-test-suite 'rule-tests)
;(textui-test-run (get-suite rule-tests))
|#
